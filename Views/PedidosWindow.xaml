<Window x:Class="WpfApp.Views.PedidosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:models="clr-namespace:WpfApp.Models"
        Title="Pedido" Height="560" Width="900" WindowStartupLocation="CenterOwner"
        Background="#FFF5F5F5">

    <Window.Resources>
        <!-- Fonte de dados para o enum FormaPagamento -->
        <ObjectDataProvider x:Key="FormaPagamentoValues"
                            MethodName="GetValues"
                            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:FormaPagamento"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <Style x:Key="ModernGroupBox" TargetType="GroupBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFE0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#FF1976D2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Margin" Value="8,4,0,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="12,8"/>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFE0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="8,4,8,4"/>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFE0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="8,4,8,4"/>
        </Style>

        <Style x:Key="ModernIntegerUpDown" TargetType="{x:Type xctk:IntegerUpDown}">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFE0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="8,4,8,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- CABEÇALHO: Pessoa + Forma de pagamento -->
        <GroupBox Header="Dados do Pedido" Style="{StaticResource ModernGroupBox}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="180"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Pessoa:" VerticalAlignment="Center" Margin="8,0,8,0" FontWeight="SemiBold"/>
                <ComboBox Grid.Column="1" Style="{StaticResource ModernComboBox}"
                          ItemsSource="{Binding PessoasCollection}"
                          SelectedItem="{Binding PessoaSelecionada}"
                          DisplayMemberPath="Nome" Height="25"/>

                <TextBlock Grid.Column="3" Text="Forma de Pagamento:" VerticalAlignment="Center" Margin="8,0,8,0" FontWeight="SemiBold"/>
                <ComboBox Grid.Column="4" Style="{StaticResource ModernComboBox}"
                          ItemsSource="{Binding Source={StaticResource FormaPagamentoValues}}"
                          SelectedItem="{Binding FormaPagamento}" Height="25"/>
            </Grid>
        </GroupBox>

        <!-- LINHA DE ADIÇÃO: Produto + Quantidade + Adicionar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="16,8,16,8">
            <TextBlock Text="Produto:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
            <ComboBox Width="308" Style="{StaticResource ModernComboBox}"
                      ItemsSource="{Binding ProdutosCollection}"
                      SelectedItem="{Binding ProdutoSelecionado}"
                      DisplayMemberPath="Nome" Height="25"/>

            <TextBlock Text="Qtd.:" VerticalAlignment="Center" Margin="16,0,8,0" FontWeight="SemiBold"/>
            <xctk:IntegerUpDown Width="80" Style="{StaticResource ModernIntegerUpDown}"
                                Minimum="1" Height="25"
                                Value="{Binding Quantidade, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Command="{Binding AdicionarItemCmd}" Style="{StaticResource ModernButton}" Margin="16,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="➕" FontSize="16" Margin="0,0,8,0"/>
                    <TextBlock Text="Adicionar" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- GRID DE ITENS -->
        <GroupBox Grid.Row="2" Header="Itens do Pedido" Style="{StaticResource ModernGroupBox}">
            <DataGrid ItemsSource="{Binding ItensCollection}"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              CanUserDeleteRows="False"
              Background="White"
              BorderBrush="#FFE0E0E0"
              BorderThickness="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Produto" Binding="{Binding ProdutoNome}" Width="*" FontSize="15">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="12,8"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Valor Unit." 
                                Binding="{Binding ValorUnitario, StringFormat=\{0:C2\},ConverterCulture='pt-BR'}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="12,8"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Qtd." Binding="{Binding Quantidade}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="12,8"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, 
                                StringFormat=\{0:C2\},ConverterCulture='pt-BR'}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="12,8"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="Ações" Width="130">
                        <DataGridTemplateColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGridTemplateColumn.HeaderStyle>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.RemoverItemCmd, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                CommandParameter="{Binding}" Style="{StaticResource ModernButton}"
                                Background="#FFD32F2F"
                                HorizontalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🗑️" FontSize="14" Margin="0,0,8,0"/>
                                        <TextBlock Text="Remover" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- RODAPÉ: Total + Finalizar/Cancelar -->
        <DockPanel Grid.Row="3" Margin="0,8,0,0">
            <TextBlock Text="{Binding ValorTotal, StringFormat='Total: {0:C2}'}"
                       FontSize="16" FontWeight="Bold" VerticalAlignment="Center" DockPanel.Dock="Left"
                       Foreground="#FF1976D2"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Command="{Binding FinalizarCmd}" Style="{StaticResource ModernButton}" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💾" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Finalizar" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding CancelarCmd}" Style="{StaticResource ModernButton}" 
                        Background="#FF757575" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="❌" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Cancelar" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>